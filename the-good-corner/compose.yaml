services:
  backend:
    build: ./backend
    ports:
      - 3000:3000
    volumes: 
      - ./backend/src/:/app/src/
  frontend:
    build: ./frontend
    ports:
      - 8080:5173
    volumes: 
      - ./frontend/src/:/app/src/